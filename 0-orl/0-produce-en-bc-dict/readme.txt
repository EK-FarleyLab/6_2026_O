easyMPRA_20231003.py is a collection of scripts written to analyze mpra data

When running the python scripts, make sure this file is in the same directory that the python scripts are called, or make sure the python code is within the PYTHONPATH environment variable.